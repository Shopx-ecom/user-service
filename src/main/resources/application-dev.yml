spring:
  datasource:
    url: jdbc:h2:mem:userdb
    driver-class-name: org.h2.Driver
    username: sameer
    password: sameer#shopx

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true